// Runtime config used in production builds (GitHub Pages, static hosting, etc.)
// Fill these values to enable login/orders area (ordini).
// NOTE: Use Supabase *anon* key only (public). Never commit service_role keys.

window.__ST_PROFUMI__ = {
  SUPABASE_URL: "",
  SUPABASE_ANON_KEY: "",
};
